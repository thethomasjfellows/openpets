[package]
name = "openpets-livekit-wake-helper"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
base64 = "0.22"
livekit-wakeword = "=0.1.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
