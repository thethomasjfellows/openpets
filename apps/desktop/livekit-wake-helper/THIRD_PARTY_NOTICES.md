# Third-party notices

The native helper uses `livekit-wakeword` under the Apache License 2.0. Its
transitive Rust dependencies and exact versions are recorded in `Cargo.lock`.

The Hey Pedra classifier has separate training provenance and evaluation data;
it may only be staged after those records and its SHA-256 digest are present.
